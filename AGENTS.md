# Repository Agent Instructions

- Always run a type check before finishing a task.
- Standard pre-finish validation for code changes should include:
  - `uv run mypy adeptly tests`
  - `uv run pytest -q`
